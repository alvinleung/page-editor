import React from "react";
import { EditableBlockProps } from "../EditableBlock";

type Props = EditableBlockProps & {};

const TestBlocks = (props: Props) => {
  return <div>TestBlocks</div>;
};

export default TestBlocks;
